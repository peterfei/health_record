<%= form_for(health_item_sub) do |f| %>
  <% if health_item_sub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(health_item_sub.errors.count, "error") %> prohibited this health_item_sub from being saved:</h2>

      <ul>
      <% health_item_sub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

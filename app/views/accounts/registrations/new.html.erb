<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Health_Record - 注册</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="renderer" content="webkit">

    <!-- The styles -->
    <%= stylesheet_link_tag 'bootstrap/bootstrap-cerulean.min' %>
    <%= stylesheet_link_tag 'charisma-app' %>
    
    <!-- jQuery -->
    <%= javascript_include_tag "application"%>
</head>
<body>
<div class="ch-container">
  <div class="row">

    <div class="row">
        <div class="col-md-12 center login-header">
            <h2>Health Record - 注册</h2>
        </div>
    </div><!--/row-->

    <div class="row">
      <div class="well col-md-5 center login-box">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="form-group">
            <%= f.label "用户名" %>
            <%= f.text_field :username, autofocus: true, class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.label "密码" %>
            <% if @minimum_password_length %>
            <em>(最少<%= @minimum_password_length %>位)</em>
            <% end %>
            <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.label "确认密码" %>
            <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
          </div>

          <div class="center col-md-8">
            <%= f.submit "注册", class: "btn btn-success" %>
            <%= link_to "返回", session_path(resource_name), class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div><!--/row-->

  </div><!--/fluid-row-->
</div><!--/.fluid-container-->
</body>
</html>
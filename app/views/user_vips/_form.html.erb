<%= form_for(user_vip) do |f| %>
  <% if user_vip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_vip.errors.count, "error") %> prohibited this user_vip from being saved:</h2>

      <ul>
      <% user_vip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
